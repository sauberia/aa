= error_messages_for @user_session

= semantic_form_for @user_session, :url => user_session_path do |f|

  = f.inputs do
    = f.input :email
    = f.input :password, :input_html => { :value => 'qweqwe' }

  = f.actions do
    = f.action :submit, :label => 'Login'